# X Master Bot 🚀

ربات اتوماسیون X برای افزایش بازدید و تعامل با پست‌ها.

## 🏰 نصب روی Oracle Cloud (توصیه شده)

### مرحله ۱: کلون کردن پروژه

```bash
git clone https://github.com/AmirCode97/X-Master-Bot.git ~/x-bot
cd ~/x-bot
```

### مرحله ۲: اجرای نصب‌کننده

```bash
chmod +x install.sh
./install.sh
```

### مرحله ۳: تنظیم کوکی‌ها

```bash
cp .env.example .env
nano .env
```

مقادیر `X_TARGET_URL`، `X_USERNAME` و `X_COOKIE_JSON` را پر کنید.

### مرحله ۴: تست دستی

```bash
chmod +x run.sh
./run.sh
```

### مرحله ۵: فعال‌سازی اجرای خودکار (هر ۲ ساعت)

```bash
chmod +x setup_cron.sh
./setup_cron.sh
```

---

## 📋 دستورات مفید

| دستور | توضیح |
|-------|-------|
| `./run.sh` | اجرای دستی ربات |
| `crontab -l` | مشاهده زمان‌بندی |
| `tail -f ~/x-bot/cron_log.txt` | مشاهده گزارش زنده |
| `crontab -r` | حذف زمان‌بندی |

---

## ⚙️ تنظیمات

فایل `config.py` را برای تغییر تنظیمات ویرایش کنید:

- `views_per_run`: تعداد بازدید در هر اجرا (پیش‌فرض: ۷)
- `min_action_delay` / `max_action_delay`: تأخیر بین اقدامات

---

## 🔐 امنیت

- فایل `.env` حاوی کوکی‌های حساس است. **هرگز آن را در گیت‌هاب آپلود نکنید.**
- آی‌پی Oracle Cloud ثابت است و ریسک کمتری نسبت به GitHub Actions دارد.
